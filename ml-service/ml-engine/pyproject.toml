[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ml-engine"
version = "0.1.0"
description = "Resume ATS ML Engine"
authors = [{name = "Athar Shaikh"}]
requires-python = ">=3.10"

dependencies = [
    "pdfminer.six",
    "python-docx"
]

[tool.setuptools.packages.find]
where = ["."]